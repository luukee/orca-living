<style>
  /* Dynamic grid system using CSS variables */
	/* mobile only */
  :root {
    --header-grid-areas: 'heading navigation';
    --header-grid-areas-mobile: 'heading search navigation';
		--columns--header: 1fr auto auto;
    --header-grid-columns: var(--columns--header);
  }
	/* tablet only */
	@media only screen and (min-width: 750px) {
		:root {
			--header-grid-areas: 'heading navigation search cart';
			--columns--header: auto auto min-content min-content;
			--header-grid-columns: 1fr auto auto auto;
		}
	}

header-drawer {
	grid-area: navigation;
  display: flex;
}
@media screen and (min-width: 750px) {
  header-drawer {
    grid-area: cart;
  }
}

.header__inner {
  display: grid;
  grid-template-areas: var(--header-grid-areas);
  grid-template-columns: var(--columns--header);
  align-items: center;
  column-gap: var(--column-gap);
}
@media screen and (max-width: 749px) {
  .header__inner {
    grid-template-areas: var(--header-grid-areas-mobile);
  }
}
.header__heading {
	grid-area: heading;
}

.header__icons {
	grid-area: icons;
}

.header__inline-menu {
	grid-area: navigation;
	justify-self: end;
}
@media only screen and (min-width: 750px) {
  .header__search {
    grid-area: search;
    justify-self: start;
  }
}
/* mobile only */
@media screen and (max-width: 990px) {
  .header__inline-menu {
    display: none;
  }
}

@media only screen and (min-width: 990px) {
  header-drawer .menu-drawer-container {
    display: none;
  }
  .header--top-left,
  .header--middle-left:not(.header--has-menu) {
    grid-template-areas: 'heading icons' 'navigation navigation';
    grid-template-columns: 1fr auto;
  }
  .header--top-left.drawer-menu,
  .header--middle-left.drawer-menu {
    grid-template-areas: 'navigation heading icons';
    grid-template-columns: auto 1fr auto;
    -moz-column-gap: 1rem;
    column-gap: 1rem;
  }
  .header--middle-left {
    grid-template-areas: 'heading navigation icons';
    grid-template-columns: auto auto 1fr;
    -moz-column-gap: 2rem;
    column-gap: 2rem;
  }
  .header--middle-center:not(.drawer-menu) {
    grid-template-areas: 'navigation heading icons';
    grid-template-columns: 1fr auto 1fr;
    -moz-column-gap: 2rem;
    column-gap: 2rem;
  }
  .header--middle-center .header__heading-link {
    text-align: center;
  }
  .header--top-center {
    grid-template-areas: 'left-icons heading icons' 'navigation navigation navigation';
  }
  .header--top-center.drawer-menu {
    grid-template-areas: 'left-icons heading icons';
    grid-template-columns: 1fr auto 1fr;
  }
  /* .header:not(.header--middle-left, .header--middle-center) .header__inline-menu {
    margin-top: 1.05rem;
  } */
}
@media only screen and (max-width: 749px) {
  .header--has-app {
    grid-template-columns: auto 1fr auto;
  }
}
@media only screen and (max-width: 989px) {
  .header--mobile-left {
    grid-template-columns: auto 2fr 1fr;
  }
}

{% if section.settings.sticky_header_type == 'reduce-logo-size' %}
  .scrolled-past-header .header__heading-logo-wrapper {
    width: 75%;
  }
{% endif %}

.menu-drawer-container {
  display: flex;
}

.list-menu {
  list-style: none;
  padding: 0;
  margin: 0;
}

.list-menu--inline {
  display: inline-flex;
  flex-wrap: wrap;
}

summary.list-menu__item {
  padding-right: 2.7rem;
}

.list-menu__item {
  display: flex;
  align-items: center;
  line-height: calc(1 + 0.3 / var(--font-body-scale));
  line-height: 1.1;
}

.list-menu__item--link {
  text-decoration: none;
  padding-bottom: 1rem;
  padding-top: 1rem;
  line-height: calc(1 + 0.8 / var(--font-body-scale));
}

@media screen and (min-width: 750px) {
  .list-menu__item--link {
    padding-bottom: 0.5rem;
    padding-top: 0.5rem;
  }
}

/* Navigation styles */
.section--navigation .list-menu {
  display: flex;
  gap: calc(var(--base-gutter--half) * 2.5);
}

.section--navigation .list-menu__item {
  padding: 1px 0;
  text-decoration: none;
  text-underline-offset: 0.3rem;
}

.section--navigation .list-menu__item:not([aria-expanded]):hover {
  text-decoration: underline;
}

.section--navigation .list-menu__item h1 {
  all: inherit;
}

/* Mobile drawer styles */
.navigation-drawer-container {
  display: flex;
}

.navigation-drawer {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100vh;
  background: rgba(var(--color-foreground), 0.04);
  z-index: 1000;
  opacity: 0;
  visibility: hidden;
  transition: opacity 0.3s ease, visibility 0.3s ease;
}

.navigation-drawer.active {
  opacity: 1;
  visibility: visible;
}

.navigation-drawer__inner-container {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgb(var(--color-background));
  transform: translateX(-100%);
  transition: transform 0.3s ease;
}

.navigation-drawer.active .navigation-drawer__inner-container {
  transform: translateX(0);
}

.navigation-drawer__header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1.5rem 2rem;
  border-bottom: 1px solid rgba(var(--color-foreground), 0.08);
}

.navigation-drawer__title {
  margin: 0;
  font-size: 1.8rem;
  font-weight: 600;
}

.navigation-drawer__close-button {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 4.4rem;
  height: 4.4rem;
  background: none;
  border: none;
  cursor: pointer;
  padding: 0;
  color: rgb(var(--color-foreground));
}

.navigation-drawer__close-button:hover {
  color: rgb(var(--color-accent));
}

.navigation-drawer__close-button .icon-close {
  width: 2rem;
  height: 2rem;
}

.navigation-drawer__navigation-container {
  height: calc(100% - 7.4rem);
  overflow-y: auto;
  padding: 2rem;
}

.navigation-drawer__menu {
  list-style: none;
  padding: 0;
  margin: 0;
}

.navigation-drawer__menu-item {
  display: block;
  padding: 1rem 0;
  border-bottom: 1px solid rgba(var(--color-foreground), 0.08);
  text-decoration: none;
  color: rgb(var(--color-foreground));
  font-size: 1.6rem;
  font-weight: 500;
}

.navigation-drawer__menu-item--active {
  color: rgb(var(--color-accent));
}

.navigation-drawer__submenu {
  padding-left: 2rem;
  margin-top: 0.5rem;
}

.navigation-drawer__submenu .navigation-drawer__menu-item {
  font-size: 1.4rem;
  padding: 0.5rem 0;
  border-bottom: none;
}

.navigation-drawer__toggle {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 4.4rem;
  height: 4.4rem;
  background: none;
  border: none;
  cursor: pointer;
  padding: 0;
}

.navigation-drawer__toggle .icon-hamburger,
.navigation-drawer__toggle .icon-close {
  width: 2rem;
  height: 2rem;
}

.navigation-drawer__toggle .icon-close {
  display: none;
}

.navigation-drawer__toggle[aria-expanded="true"] .icon-hamburger {
  display: none;
}

.navigation-drawer__toggle[aria-expanded="true"] .icon-close {
  display: block;
}

/* Hide desktop navigation on mobile */
@media screen and (max-width: 749px) {
  .navigation-desktop {
    display: none;
  }
}

/* Show desktop navigation on larger screens */
@media screen and (min-width: 750px) {
  .navigation-mobile {
    display: none;
  }
}
.header__inner {
  padding-top: {{ section.settings.padding_top | times: 0.5 | round: 0 }}px;
  padding-bottom: {{ section.settings.padding_bottom | times: 0.5 | round: 0 }}px;
}

.section-header {
  position: sticky;
  /* This is for fixing a Safari z-index issue. PR #2147 */
  margin-bottom: {{ section.settings.margin_bottom | times: 0.75 | round: 0 }}px;
}

@media screen and (min-width: 750px) {
  .section-header {
    margin-bottom: {{ section.settings.margin_bottom }}px;
  }
}

@media screen and (min-width: 990px) {
  .header__inner {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }
}
.section-header .header__inline-menu {
  margin: 0;
}
/* @media only screen and (min-width: 750px) {
  .section-header .header__inline-menu {
    display: block !important;
  }
}
@media only screen and (min-width: 990px) {
  .section-header .header__inline-menu {
    justify-self: end;
  }
} */
.section-header .header__inline-menu .list-menu {
  display: block !important;
}
.section-header .header__inline-menu .list-menu .menu-item__name-cart {
  display: none;
}
.section-header .header__inline-menu .list-menu :nth-last-child(2) {
  display: initial;
}
@media only screen and (max-width: 749px) {
  .section-header .header__inline-menu .list-menu .menu-item__name-cart {
    display: initial;
  }
  .section-header .header__inline-menu .list-menu :nth-last-child(2) {
    display: none;
  }
}
.section-header .header__inline-menu .list-menu .header__menu-item {
  padding: 1px 0;
}
.section-header
  .header__inline-menu
  .list-menu
  .header__menu-item
  .header__active-menu-item:not(:hover) {
  text-decoration: none;
}
.section-header
  .header__inline-menu
  .list-menu
  .header__menu-item
  .header__active-menu-item:before,
.navigation-secondary
  .list-menu__item.menu-item--active:before {
  content: '_';
}
@media only screen and (max-width: 749px) {
  .section-header .header__inline-menu .list-menu {
    display: grid !important;
    -moz-column-gap: 20px;
    column-gap: 20px;
    row-gap: 5px;
    grid-template-columns: repeat(2, 1fr);
    grid-template-rows: repeat(3, 1fr);
    grid-auto-flow: column;
  }
}
@media only screen and (min-width: 750px) {
  .section-header .header__inline-menu .list-menu {
    columns: 2;
    column-gap: var(--columns--gap);
  }
}
.section-header .header-wrapper--border-bottom {
  border-bottom: 1px solid #000;
}
</style>